var assert = require('assert');

describe('mocha', function(){
	describe('assert', function(){
		it('should pass valid conditions', function(){
			assert.equal(true, true);
		});

		/*it('should fail invalid conditions', function(){
			assert.equal(true, false);
		});*/
	});
});
